PROPERTY MAINTENANCE — DIRECT INDEX UPDATE

Open:
courses > pmo

Upload and replace every file in this ZIP, including index.html.

Commit message:
Update Property Maintenance directly to match Bench

This version links the dashboard, navigation, local reminders, profile controls and setup prompt directly inside index.html, exactly like the working Brick fix. It no longer relies on the service worker injecting those files.
